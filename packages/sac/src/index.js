import {
    bodyRouter,
    createNewEvent,
    forEach,
    isDisplayed,
    query,
    requestAnimFrame,
    supportsWebp,
    throttle,
} from './core';

import { reverseString } from './parsing';

import { roundNumbers } from './math';

export default {
    bodyRouter,
    createNewEvent,
    forEach,
    isDisplayed,
    query,
    requestAnimFrame,
    reverseString,
    roundNumbers,
    supportsWebp,
    throttle,
};
