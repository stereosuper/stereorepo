import {
    bodyRouter,
    createNewEvent,
    forEach,
    isDisplayed,
    query,
    requestAnimFrame,
    throttle,
} from './core';

import { reverseString } from './parsing';

import { roundNumbers } from './math';

export default {
    roundNumbers,
    forEach,
    reverseString,
    createNewEvent,
    requestAnimFrame,
    throttle,
    query,
    isDisplayed,
    bodyRouter,
};
