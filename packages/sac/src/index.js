import {
    bodyRouter,
    createNewEvent,
    forEach,
    isDisplayed,
    query,
    requestAnimFrame,
    throttle,
} from './core';

import { roundNumbers } from './math';

import { reverseString } from './parsing';

export default {
    roundNumbers,
    forEach,
    reverseString,
    createNewEvent,
    requestAnimFrame,
    throttle,
    query,
    isDisplayed,
    bodyRouter,
};
